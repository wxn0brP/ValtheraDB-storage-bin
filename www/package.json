{
    "name": "www",
    "version": "0.0.1",
    "main": "dist/index.js",
    "type": "module",
    "license": "MIT",
    "scripts": {
        "build": "bun build src/index.ts --outdir dist --minify --sourcemap && sass public:dist",
        "start": "node dist/server.js",
        "bun": "bun run src/server.ts"
    },
    "devDependencies": {
        "@wxn0brp/flanker-ui": "^0.5.1",
        "sass": "^1.99.0"
    },
    "dependencies": {
        "@wxn0brp/falcon-frame": "^0.9.0"
    }
}
